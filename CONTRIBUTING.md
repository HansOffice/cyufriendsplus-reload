# Contributing

谢谢你愿意帮 CyuFriends Reload 变得更好

这个项目更看重真实服务器里的体验：玩家点开菜单是否顺手，命令反馈是否清楚，数据库操作会不会卡住主线程，配置是否能让服主看懂。提交 PR 时，也请优先从这些角度说明你的改动

## 开发流程

1. Fork 项目并创建自己的分支
2. 修改代码或资源文件
3. 运行构建：

```bash
mvn -DskipTests package
```

4. 如果改了 GUI、命令、权限或配置，请在 PR 里写清楚玩家应该怎么测试

## 适合贡献的方向

- 修复真实游戏内 bug
- 改善 GUI 操作路径和提示文案
- 补充 PlaceholderAPI 变量
- 增加 API 示例
- 优化数据库查询、缓存和线程调度
- 补充面向服主的配置样例

## 代码风格

- Kotlin 代码保持现有模块结构
- 数据库写入和较重查询不要放在玩家主线程
- GUI 行为优先通过 `gui/*.yml` 配置表达
- 面向玩家的文本放进 `messages.yml`，不要散落在代码里
- 不要在 PR 中混入无关格式化或 IDE 文件

## 跨服说明

1.0 首发按单服发布。`proxy` 模块代码保留，但代理端插件暂不随 1.0 发布。与跨服相关的 PR 可以讨论，但请不要把它当作当前稳定功能
