target/
.idea/
.internal-docs/
*.iml
*.log
dependency-reduced-pom.xml

# Local compile-only jars.
# PlaceholderAPI resolves from Maven now; cyuid-reload is still a local companion jar.
libs/*.jar
!libs/cyuid-reload-1.0.3.jar
